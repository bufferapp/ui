import React from 'react';
import Button from '@bufferapp/components/Button';

/** Primary type buttons */
export default function ExamplePrimary() {
    return [
        <Button primary large onClick={()=>{}}>Primary Large</Button>,
        <Button primary onClick={()=>{}}>Primary Button</Button>,
        <Button primary small onClick={()=>{}}>Primary Small</Button>
   ]
}



