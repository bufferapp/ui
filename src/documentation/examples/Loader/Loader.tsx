import React from 'react'
// @ts-expect-error TS(2307) FIXME: Cannot find module '@bufferapp/ui/Loader' or its c... Remove this comment to see the full error message
import Loader from '@bufferapp/ui/Loader'

/** Loader Example */
export default function ExampleLoader() {
  return <Loader />
}
